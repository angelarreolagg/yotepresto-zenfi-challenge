/**
 * expense-breakdown — the month's spend as a donut, a ranked category list, an interpretive
 * insight sentence, and the income/net summary strip. Answers G1.
 * Depends on: @/entities/transaction, @/shared/*
 */
export { BreakdownSection } from './ui/BreakdownSection';
