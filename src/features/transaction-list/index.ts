/**
 * transaction-list — the month's movements: search, three filters, weekly navigation and
 * category correction from a modal (this is where G2 lives).
 * Depends on: @/entities/transaction, @/shared/*
 */
export { TransactionList } from './ui/TransactionList';
