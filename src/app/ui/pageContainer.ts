/**
 * Page width decided in one place, shared by the sticky TopBar and the content so their left
 * edges cannot drift apart (ROADMAP §6).
 */
export const PAGE_CONTAINER =
  'mx-auto w-full max-w-md px-4 sm:max-w-2xl sm:px-6 md:max-w-3xl lg:max-w-6xl lg:px-8 xl:max-w-7xl';
